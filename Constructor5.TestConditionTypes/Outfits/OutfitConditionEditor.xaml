<UserControl x:Class="Constructor5.TestConditionTypes.Outfits.OutfitConditionEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Constructor5.TestConditionTypes.Outfits"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:shared="clr-namespace:Constructor5.UI.Shared;assembly=Constructor5.UI"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <StackPanel>
        <CheckBox Content="Everyday" IsChecked="{Binding IncludeEveryday}" />
        <CheckBox Content="Formal" IsChecked="{Binding IncludeFormal}" />
        <CheckBox Content="Athletic" IsChecked="{Binding IncludeAthletic}" />
        <CheckBox Content="Sleepwear" IsChecked="{Binding IncludeSleepwear}" />
        <CheckBox Content="Party" IsChecked="{Binding IncludeParty}" />
        <CheckBox Content="Swimwear" IsChecked="{Binding IncludeSwimwear}" />
        <CheckBox Content="Hot Weather" IsChecked="{Binding IncludeHotWeather}" />
        <CheckBox Content="Cold Weather" IsChecked="{Binding IncludeColdWeather}" />
        <CheckBox Content="Career" IsChecked="{Binding IncludeCareer}" />
        <CheckBox Content="Batuu" IsChecked="{Binding IncludeBatuu}" />
        <CheckBox Content="Nude" IsChecked="{Binding IncludeNude}" />
        <CheckBox Content="Situation" IsChecked="{Binding IncludeSituation}" />
        <CheckBox Content="Special" IsChecked="{Binding IncludeSpecial}" />
        <CheckBox Content="Inverted (disallow the selected outfit types)" IsChecked="{Binding Inverted}" />
        <shared:Field Label="Participant" LabelWidth="100">
            <shared:ParticipantControl Text="{Binding Participant}" />
        </shared:Field>
    </StackPanel>
</UserControl>
