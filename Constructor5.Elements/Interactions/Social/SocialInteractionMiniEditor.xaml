<UserControl x:Class="Constructor5.Elements.Interactions.Social.SocialInteractionMiniEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:shared="clr-namespace:Constructor5.UI.Shared;assembly=Constructor5.UI"
             x:Name="uc"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <StackPanel>
        <shared:Field Label="Name" LabelWidth="150">
            <shared:STBLStringControl Text="{Binding SIInfoComponent.Name, ElementName=uc}" UpdateLabelForElement="{Binding}" />
        </shared:Field>
        <shared:Field Label="Name On Target" LabelWidth="150">
            <shared:STBLStringControl Text="{Binding SIInfoComponent.NameOnTarget, ElementName=uc}" />
        </shared:Field>
        <shared:Field Label="Interaction Type" LabelWidth="150">
            <StackPanel>
                <shared:SelectableObjectControl EditorCategory="Invalid"
                                                Object="{Binding SITemplateComponent.Template, ElementName=uc}"
                                                TypeRegistryCategory="SocialInteractionTemplates" />
            </StackPanel>
        </shared:Field>
        <shared:ObjectEditorControl EditorCategory="Default"
                                    EditorTag="AssignedInteraction"
                                    Object="{Binding SITemplateComponent.Template, ElementName=uc}" />
    </StackPanel>
</UserControl>
