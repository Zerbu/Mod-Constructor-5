<UserControl x:Class="Constructor5.Elements.SituationJobs.Components.SituationJobUniformEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Constructor5.Elements.SituationJobs.Components"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:shared="clr-namespace:Constructor5.UI.Shared;assembly=Constructor5.UI"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <StackPanel>
        <shared:Field Label="Outfit Change Reason" LabelWidth="125">
            <shared:TextBoxWithPresetsControl PresetsFolder="OutfitChangeReason" Text="{Binding OutfitChangeReason}" />
        </shared:Field>
        <CheckBox IsChecked="{Binding HigherPriority}">
            <StackPanel>
                <TextBlock Text="Higher Priority" />
                <TextBlock FontStyle="Italic"
                           Text="If checked, the outfit for this situation will be prioritized over other running situations. This should usually be left unchecked except in special cases."
                           TextWrapping="Wrap" />
            </StackPanel>
        </CheckBox>
    </StackPanel>
</UserControl>
