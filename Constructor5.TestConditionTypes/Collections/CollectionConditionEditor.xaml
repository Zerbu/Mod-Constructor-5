<UserControl x:Class="Constructor5.TestConditionTypes.Collections.CollectionConditionEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Constructor5.TestConditionTypes.Collections"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:shared="clr-namespace:Constructor5.UI.Shared;assembly=Constructor5.UI"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <StackPanel>
        <shared:Field Label="Collection Type" LabelWidth="150">
            <shared:TextBoxWithPresetsControl PresetsFolder="Collection" Text="{Binding CollectionType}" />
        </shared:Field>
        <CheckBox Content="Require the collection to be complete" IsChecked="{Binding RequireCollectionToBeComplete}" />
        <shared:Field Label="Threshold"
                      LabelWidth="150"
                      Visibility="{Binding RequireCollectionToBeComplete, Converter={StaticResource BoolToVisInverted}}">
            <shared:ThresholdControl Threshold="{Binding Threshold}" />
        </shared:Field>
        <shared:Field Label="Participant" LabelWidth="150">
            <shared:ParticipantControl Text="{Binding Participant}" />
        </shared:Field>
    </StackPanel>
</UserControl>
